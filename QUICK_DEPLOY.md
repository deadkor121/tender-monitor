# 🚀 Быстрый деплой на Render.com

Этот файл поможет вам загрузить проект на GitHub для деплоя на Render.com

## ⚡ Оптимизация для Render Free Plan

**ВАЖНО:** Проект оптимизирован для бесплатного плана Render:
- Используется `puppeteer-core` + `@sparticuz/chromium` вместо полного Puppeteer
- Это ускоряет развертывание с ~5-10 минут до ~2-3 минут
- Предотвращает ошибки таймаута (Exit code 143)
- Экономит память и ресурсы

## Шаг 1: Подготовка проекта

1. Убедитесь, что все файлы сохранены
2. Проверьте файл `.gitignore` - он должен исключать:
   - `node_modules/`
   - `.env`
   - `data/`

## Шаг 2: Загрузка на GitHub

### Вариант A: Через командную строку

```bash
# 1. Инициализация Git (если еще не сделано)
git init

# 2. Добавить все файлы
git add .

# 3. Создать первый коммит
git commit -m "Initial commit: Tender Monitor System"

# 4. Создать репозиторий на GitHub
# Перейдите на https://github.com/new
# Создайте репозиторий с именем: tender-monitor
# НЕ добавляйте README или .gitignore (они уже есть)

# 5. Связать с GitHub
git remote add origin https://github.com/deadkor121/tender-monitor.git

# 6. Загрузить код
git branch -M main
git push -u origin main
```

### Вариант B: Через GitHub Desktop

1. Скачайте GitHub Desktop: https://desktop.github.com/
2. Откройте приложение
3. File → Add Local Repository
4. Выберите папку `tender-monitor`
5. Publish Repository → Создайте репозиторий
6. Снимите галочку "Keep this code private" (если хотите публичный)
7. Нажмите "Publish repository"

## Шаг 3: Деплой на Render.com

### 3.1 Регистрация

1. Перейдите на https://render.com
2. Нажмите **Get Started**
3. Выберите **Sign up with GitHub**
4. Авторизуйтесь через GitHub
5. Дайте разрешение Render на доступ к репозиториям

### 3.2 Создание PostgreSQL базы

1. В Dashboard нажмите **New +**
2. Выберите **PostgreSQL**
3. Заполните:
   - **Name**: `tender-monitor-db`
   - **Database**: `tender_monitor`
   - **User**: `tender_user` (автоматически)
   - **Region**: **Frankfurt** (ближе к Норвегии)
   - **PostgreSQL Version**: 17
   - **Plan**: **Free**
4. Нажмите **Create Database**
5. ⏳ Подождите 1-2 минуты пока создается
6. 📋 **СКОПИРУЙТЕ "Internal Database URL"** - он понадобится!

Пример Internal URL:
```
postgresql://tender_user:abc123xyz@dpg-xxx-xxx.frankfurt-postgres.render.com/tender_monitor
```

### 3.3 Создание Web Service

1. Нажмите **New +**
2. Выберите **Web Service**
3. Найдите и выберите репозиторий **tender-monitor**
4. Нажмите **Connect**
5. Заполните настройки:

**Basic Settings:**
- **Name**: `tender-monitor` (будет URL: tender-monitor.onrender.com)
- **Region**: **Frankfurt**
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Runtime**: **Node**

**Build & Deploy:**
- **Build Command**: `npm install`
- **Start Command**: `node src/server.js`

**Plan:**
- Выберите **Free** ($0/месяц)

6. Прокрутите вниз до **Environment Variables**
7. Нажмите **Add Environment Variable** и добавьте:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `3001` |
| `DATABASE_URL` | *[вставьте Internal Database URL из шага 3.2]* |
| `ANBUD_USERNAME` | *[ваш email от Anbud Direkte]* |
| `ANBUD_PASSWORD` | *[ваш пароль от Anbud Direkte]* |
| `HEADLESS` | `true` |
| `CHECK_INTERVAL_MINUTES` | `30` |

**Опционально (если используете):**
| Key | Value |
|-----|-------|
| `TELEGRAM_ENABLED` | `true` |
| `TELEGRAM_BOT_TOKEN` | *[токен бота]* |
| `TELEGRAM_CHAT_ID` | *[ваш chat ID]* |

8. Нажмите **Create Web Service**

### 3.4 Процесс деплоя

⏳ Render начнет автоматический деплой:

```
==> Cloning from GitHub...
==> Building application...
==> Installing dependencies (npm install)...
==> Downloading Chromium for Puppeteer...
==> Starting application (node src/server.js)...
==> Deploy successful! 🎉
```

**Время деплоя:** 5-10 минут

### 3.5 Проверка работы

1. После успешного деплоя вы получите URL:
   ```
   https://tender-monitor.onrender.com
   ```

2. Откройте URL в браузере
3. Должна загрузиться страница мониторинга
4. Первая загрузка может занять 30-60 секунд (cold start)

### 3.6 Проверьте логи

1. В Render Dashboard откройте ваш Web Service
2. Перейдите во вкладку **Logs**
3. Должны увидеть:
   ```
   [Server] Запущен на порту 3001
   [DB] PostgreSQL подключена успешно
   [Scheduler] Проверка запланирована каждые 30 минут
   ```

## Шаг 4: Тестирование

После деплоя проверьте:

✅ **Интерфейс загружается**
✅ **Статистика отображается**
✅ **Кнопка "Проверить сейчас" работает**
✅ **Тендеры загружаются из БД**
✅ **Добавление в избранное работает**
✅ **Аналитика открывается**

## Шаг 5: Настройка автодеплоя

После первого деплоя, при каждом `git push`:
1. Render автоматически обнаружит изменения
2. Запустит новую сборку
3. Развернет обновленную версию

```bash
# Внесли изменения в код
git add .
git commit -m "Добавил новую функцию"
git push

# Render автоматически задеплоит изменения!
```

## 🐛 Решение проблем

### Ошибка: "Build failed"
- Проверьте логи сборки в Render Dashboard
- Убедитесь, что все зависимости в `package.json`
- Проверьте Build Command: должен быть `npm install`

### Ошибка: "Application failed to respond"
- Проверьте Environment Variables
- Убедитесь, что PORT = 3001
- Проверьте Start Command: `node src/server.js`

### База данных не подключается
- Проверьте DATABASE_URL
- Должен быть **Internal** URL (не External)
- База и сервис должны быть в одном регионе (Frankfurt)

### Puppeteer не работает
- На Free плане может быть медленным
- Некоторые сайты блокируют Render IP
- Попробуйте установить `HEADLESS=new` вместо `true`

### Сервис засыпает
- Это нормально для Free плана (15 минут неактивности)
- Первое открытие после сна: 30-60 секунд
- Решение: апгрейд до $7/месяц или используйте UptimeRobot для пинга

## 💡 Советы

1. **Держите DATABASE_URL в секрете** - не коммитьте в Git
2. **Используйте Internal Database URL** - быстрее и безопаснее
3. **Мониторьте логи** - первые дни проверяйте ошибки
4. **Тестируйте локально** перед push в main
5. **Делайте backup БД** - экспортируйте данные регулярно

## 📊 Мониторинг использования

В Render Dashboard → Metrics вы увидите:
- **Requests** - количество запросов
- **Response Time** - скорость ответа
- **Memory Usage** - использование RAM (лимит 512MB на Free)
- **Build Time** - время сборки

## 🔄 Обновление проекта

Когда вы делаете изменения:

```bash
# 1. Внесли изменения в код локально
# 2. Тестируете: npm start
# 3. Коммитите:
git add .
git commit -m "Описание изменений"
git push

# 4. Render автоматически задеплоит через 5-10 минут
```

## ⚡ Что дальше?

После успешного деплоя:
- ✅ Добавьте сайт в закладки
- ✅ Протестируйте все функции
- ✅ Настройте Telegram уведомления
- ✅ Установите как PWA на телефон
- ✅ Поделитесь проектом с командой

## 🎉 Готово!

Ваш Tender Monitor теперь доступен онлайн 24/7!

**URL**: `https://tender-monitor.onrender.com`

---

Если возникли проблемы - смотрите детальную инструкцию: [DEPLOY.md](./DEPLOY.md)
