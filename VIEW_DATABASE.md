# 🔍 Просмотр SQLite базы данных

## Проблема

SQLite CLI (`sqlite3`) не установлен в вашей системе Windows по умолчанию.

## ✅ Решение: Утилита view-db.js

Создана Node.js утилита для удобного просмотра базы данных!

## 📖 Команды

### 1. Статистика (по умолчанию)
```bash
node view-db.js
# или
node view-db.js stats
```
Показывает:
- Общее количество тендеров
- Распределение по источникам
- Топ-10 категорий
- Временная статистика

### 2. Просмотр тендеров
```bash
node view-db.js tenders          # Последние 10
node view-db.js tenders 20       # Последние 20
node view-db.js tenders 50       # Последние 50
```

### 3. Избранное
```bash
node view-db.js favorites
```

### 4. Поиск
```bash
node view-db.js search "строительство"
node view-db.js search "bygg"
node view-db.js search "framework"
```

### 5. Структура БД
```bash
node view-db.js tables
```
Показывает все таблицы и количество записей.

### 6. Помощь
```bash
node view-db.js help
```

## 🎯 Примеры использования

```bash
# Быстрый просмотр статистики
node view-db.js

# Последние 5 тендеров
node view-db.js tenders 5

# Поиск тендеров по строительству
node view-db.js search "bygg"

# Посмотреть что в избранном
node view-db.js favorites

# Структура БД
node view-db.js tables
```

## 💡 Альтернативы

### Установить SQLite CLI (опционально)

**Вариант 1: Chocolatey** (рекомендуется)
```powershell
# Установить Chocolatey (если нет)
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# Установить SQLite
choco install sqlite
```

**Вариант 2: Вручную**
1. Скачать: https://www.sqlite.org/download.html
2. Распаковать `sqlite-tools-win32-x86-*.zip`
3. Добавить в PATH или скопировать в папку проекта

### GUI инструменты

1. **DB Browser for SQLite** (бесплатно, рекомендуется)
   - Скачать: https://sqlitebrowser.org/
   - Простой и удобный интерфейс
   - Визуализация таблиц, редактирование данных

2. **DBeaver** (бесплатно)
   - Скачать: https://dbeaver.io/
   - Профессиональный инструмент
   - Поддержка множества БД

3. **DataGrip** (платно)
   - От JetBrains
   - Самый мощный функционал

## 📊 Текущее состояние БД

```
База данных: data/tenders.db (155 KB)

Статистика:
  📦 Всего тендеров: 77
  ⭐ Избранных: 0
  
  По источникам:
    • anbud  : 50 (64.9%)
    • ted    : 15 (19.5%)
    • doffin : 12 (15.6%)
```

## 🔧 Если нужно выполнить SQL запросы

Создайте файл `query.js`:

```javascript
const DatabaseService = require('./src/databaseService');
const db = new DatabaseService();

// Ваш SQL запрос
const result = db.db.prepare(`
  SELECT source, COUNT(*) as count 
  FROM tenders 
  GROUP BY source 
  ORDER BY count DESC
`).all();

console.log(result);
db.close();
```

Запуск:
```bash
node query.js
```

---

**Готово!** Теперь вы можете легко просматривать данные в БД! 🎉
