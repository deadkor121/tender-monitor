# 🔐 Решение проблемы с паролем PostgreSQL

## Проблема
При подключении к PostgreSQL через pgAdmin возникает ошибка аутентификации.

## ✅ Решения

### Решение 1: Вспомнить пароль
Попробуйте вспомнить пароль, который вводили при установке PostgreSQL 17.

В pgAdmin введите:
- **Host**: `localhost`
- **Port**: `5432`
- **Database**: `postgres`
- **User**: `postgres`
- **Password**: `ваш_пароль`

---

### Решение 2: Сбросить пароль через psql

1. Откройте PowerShell **от администратора**

2. Подключитесь к PostgreSQL:
```powershell
& "C:\Program Files\PostgreSQL\17\bin\psql.exe" -U postgres
```

3. Введите новый пароль:
```sql
ALTER USER postgres WITH PASSWORD 'новый_пароль';
```

4. Выход:
```sql
\q
```

5. Попробуйте подключиться в pgAdmin с новым паролем

---

### Решение 3: Настроить trust-аутентификацию (БЕЗ ПАРОЛЯ)

⚠️ **Только для локальной разработки!**

1. Откройте файл:
```
C:\Program Files\PostgreSQL\17\data\pg_hba.conf
```

2. Найдите строки:
```
# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
```

3. Замените `scram-sha-256` на `trust`:
```
# IPv4 local connections:
host    all             all             127.0.0.1/32            trust
```

4. Сохраните файл

5. Перезапустите PostgreSQL:
```powershell
Restart-Service postgresql-x64-17
```

6. Теперь можно подключаться БЕЗ ПАРОЛЯ

---

### Решение 4: Использовать SQLite (текущая БД)

SQLite уже работает отлично! Можете продолжить использовать SQLite:
- ✅ Уже настроено
- ✅ 77 тендеров в БД
- ✅ Все функции работают
- ✅ Не требует пароля

Просто запустите:
```bash
npm start
```

PostgreSQL нужен только если планируете:
- Очень большие объемы (>100k записей)
- Несколько одновременных пользователей
- Репликацию данных

---

## 🎯 Рекомендация

**Для вашего проекта SQLite достаточно!**

Если все же хотите PostgreSQL:
1. Используйте **Решение 2** (сбросить пароль)
2. Или **Решение 3** (trust для разработки)

Выберите удобный вариант! 😊
